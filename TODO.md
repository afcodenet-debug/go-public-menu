- [ ] Corriger `vite.config.ts` pour que le renderer Vite sorte dans `dist/renderer` (boot prod compatible Electron)
- [ ] Ajouter une config `electron-builder` dans `package.json` (inclure `dist/main`, `dist/renderer`, `dist/server`)
- [ ] Réparer `src/main/main.js` :
  - charger le renderer via chemin bootstrappé
  - démarrer le backend Express depuis `dist/server/server/server.js` en mode packaged
  - s’assurer que `BrowserWindow` est créé correctement (fermetures manquantes) sans toucher la logique métier
- [ ] `npm run build` puis `npm run dist`
- [ ] Lancer l’exécutable généré (macOS/Windows) et valider `/health` + endpoints UI->API
